import 'package:flutter/material.dart';

abstract final class ClueColors {
  // Core palette — from design spec
  static const ink = Color(0xFF1A1714);
  static const paper = Color(0xFFF7F2EA);
  static const paperDeep = Color(0xFFFBF7F0);
  static const amber = Color(0xFFE8813C);
  static const clay = Color(0xFFC15F45);

  // Surfaces / borders
  static const border = Color(0xFFE9E0D1);
  static const borderStrong = Color(0xFFE3DACB);
  static const muted = Color(0xFF9C9384);
  static const mutedDark = Color(0xFF8A8172);
  static const iconBg = Color(0xFFF4E6D5);

  // Dark mode surfaces
  static const inkSurface = Color(0xFF241F1B);
  static const inkCard = Color(0xFF2E2820);

  // Semantic
  static const userDot = Color(0xFF4A90D9);
}
